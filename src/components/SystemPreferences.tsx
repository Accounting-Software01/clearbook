import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const SystemPreferences = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>System Preferences</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Manage system-wide preferences.</p>
            </CardContent>
        </Card>
    );
};

export default SystemPreferences;
