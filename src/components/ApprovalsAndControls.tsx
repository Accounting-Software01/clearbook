import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ApprovalsAndControls = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Approvals & Controls</CardTitle>
            </CardHeader>
            <CardContent>
                <p>Manage approval workflows and controls.</p>
            </CardContent>
        </Card>
    );
};

export default ApprovalsAndControls;
